/**
 * Icons.
 *
 * `lucide-react` is a dependency, but it exports every icon as a separate module
 * and importing it from a dozen files scatters the icon vocabulary across the
 * codebase. Re-exporting the set the design actually uses keeps that vocabulary
 * in one place and makes an unused icon obvious.
 */

export {
  AlertCircle,
  ArrowLeft,
  ArrowRight,
  Calendar,
  CalendarDays,
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  Clock,
  Inbox,
  LayoutDashboard,
  Loader2,
  LogOut,
  Mail,
  MapPin,
  Menu,
  Phone,
  Plus,
  Scissors,
  Search,
  Settings,
  Sparkles,
  Star,
  Trash2,
  TrendingUp,
  User,
  Users,
  X,
} from "lucide-react";
